"""
[지시사항]
1. cal.py에 정의된 함수의 매개변수의 대입 연산자 양 옆의 띄어쓰기를 제거하세요.
2. cal.py에 정의된 함수 내 이항 연산자와 변수 사이의 띄어쓰기를 추가하세요.
3. cal.py의 bigger_check() 함수에서 어느 조건에도 속하지 않는 경우 None을 반환하는 코드를 작성하세요.
4. main.py에 한 줄로 import된 두 모듈을 두 줄로 나누세요.
5. main.py에서 변수를 정의할 때 대입 연산자의 띄어쓰기를 추가하세요.
6. main.py에서 함수를 호출할 때 함수명과 괄호의 띄어쓰기를 제거하세요.
"""

import cal
import math

var1 = 1
var2 = 2
var3 = 3

res1 = cal.plus(var1, var2)
res2 = cal.minus(var1, var2)
res3 = math.factorial(var3)
